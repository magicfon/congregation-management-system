# 地圖分配系統 — 領域詞彙表

## 核心概念

**分區候選（Boundary candidate）** — 由三張原圖的淡紫色界線擷取而成，使用左上原點、向下 y 軸的影像像素 `[x,y]`，與舊 areas JSON 的 `[y,x]` 不同；尚未人工確認，不得直接覆寫正式 polygon。詳見 ADR-0009。

**分區修正草稿（Boundary draft）** — 管理員補線切割與拖曳頂點後的候選 JSON。使用者要求跨裝置共用，儲存在 Neon 獨立表，以版本號避免覆蓋他人更新；不改動分發、回報或正式區域。詳見 ADR-0010。

- 調整頂點模式支援點選後刪除（按鈕或 Delete），每環至少保留三個頂點；拒絕無效邊界，刪除可復原並隨草稿存到雲端。
- 分區編輯預設自動載入雲端，按「儲存」即寫雲端；裝置備份只在異常或發現未存內容時提示恢復，正常流程不區分本機與雲端。

**區域（Area）** — 傳道領土的最小單位。每個區域有編號（如 A-12）、所屬地圖（楠梓/橋頭/梓官）、以及可選的 polygon 邊界。

**區塊（Block）** — 區域的分组單位。一個 blockCode（如 A-1）可包含多個區域，分配時一次分一整組給一人。

**分發（Dispatch）** — 將一組區域指派給某個成員。分發時自動記錄 `dispatchedAt`。

**收回（Collect）** — 管理員將已分發的區域從成員手中收回。收回時設定 `completedAt`。

**分發中（isDispatched）** — 計算欄位：`dispatchedAt != null && (completedAt == null || completedAt < dispatchedAt)`。只有「真實分發中」的區域才算。

**分配目的（assignNote）** — 可選的分配備註。null 代表沒有特別目的。

## LINE 帳號配對（2026-09-20 確認）

- 記錄 LINE 顯示名稱與 UID，不收集搜尋好友用的 LINE ID；每次 LINE 登入更新顯示名稱，不覆寫成員姓名。
- 管理員可將已登入帳號的 UID 配對至既有成員；一名成員只能綁定一個 UID，已綁定者不直接覆蓋。
- 配對只移動登入綁定，不搬動或刪除原成員的地圖、回報等資料；目標成員保留姓名、角色與資料。
- 配對後原 LINE 工作階段失效，使用者重新登入後取得目標成員身分。

## 角色

- 共用介面顯示目前登入者的成員姓名與角色（管理員／長老／一般成員），以 `/api/me` 為來源；桌面右上角與手機頂部皆可見，收合側欄不影響顯示。

**管理員（admin）** — 可執行收回、轉移等操作。目前只有 admin 角色。

**一般成員** — 所有 LINE 登入的使用者。可查看地圖分配狀態和自己被分到的區域，不能操作收回/轉移。

## 操作規則

- 收回 = 設定 completedAt = now()，清除 assignedMemberId
- 轉移 = 先收回再重新分發（兩步操作）
- 批量收回 = 支援一次收回多張
- 分發天數計算 = now() - dispatchedAt（只在有真實 dispatchedAt 時才算）
- 所有 LINE 登入者可看，只有 admin 可操作

## 使用中地圖瀏覽（2026-09-20 確認）

- 「人員分配」併入「使用中地圖」：管理員可逐張勾選、全選某人目前篩選的地圖並批量收回；一般成員僅能瀏覽。
- 改變篩選後不保留被隱藏地圖的勾選，收回前顯示確認；舊 `/assignments` 網址轉向 `/active-assignments`。

- 人員摘要先顯示張數與領取狀況，點選人員才在該人員下方展開明細，再點一次收合。
- 明細每張地圖一列，分開顯示「地區＋地圖號」與「已領取天數」；手機同樣就地展開。
- 已領取天數 = 現在減去 dispatchedAt 的完整天數，不是距離上次回報的天數。
- 地圖依領取最久排序，無日期排最後並標示「分發日未記錄」；30–59 天黃色、60 天以上紅色。

## 地圖分配清單（2026-09-20 確認）

- 列出所有地圖，固定按地區、區塊、地圖編號排列，保留區域性；不按閒置天數排序。
- 清單採緊湊連續列，不顯示 A-1／A-2 等中分組標題與分組選取列；保留原排序，逐張勾選後批量分發。
- 閒置時間指上次回報完成日至今的台北日曆天數，來源為 Sheet「區域狀態」E 欄，不使用最後操作時間或收回時間。
- `lastReportedCompletedAt` 獨立保存該日期；分發與收回不得重設。尚未同步與無回報紀錄分別標示。
- 管理員勾選可分發地圖 → 選啟用中成員 → 一次分發；使用中地圖不可勾選，送出時再次檢查並整批原子寫入。
- 以顏色標示 90–179 天與 180 天以上，僅協助閱讀，不影響順序；提供地區與只看可分發篩選。

## 資料來源與 Sheet 對接（2026-08-16 確認）

**傳道區域回報 Spreadsheet**（`1Dt4YvBIhk5u70NzVVA36ya5C8Rpu3SctqQmwu3i4HGU`）— 大家平常看的正式版本：

- **「傳道區域回報」工作表** — Google Form 回報流水（append-only）。欄位：時間戳記 / 傳道員姓名 / 傳道時段 / 區域號碼 / 開始日期 / 結束日期 / 備註。2022/9 起使用，626 筆，最新 2026/8/14，活躍中。**區域號碼是全域 1-213**（楠梓含個人區域/海總 1-89、橋頭 60-118 左右、梓官 119-213…實際按 blockCode 對應）。
- **「區域狀態」工作表** — 現況表（R1 標題 + R2-R214 共 213 區域）。欄位：區塊編碼 / 區域號碼 / 負責弟兄 / 分發日期 / 最後完成日 / 幾天沒傳了? / 疫情後完成次數。E/F/G 欄是公式（從回報流水算最後完成日/閒置天數/次數），**A/B/C/D 欄是手動維護的**（負責弟兄 + 分發日期）。
- 其他工作表（花費天數圖表 / ID List / 地圖管理人 / 編碼分析 / 樞紐分析）是分析用副本/圖表，不動。

**號碼對應**：Sheet 的全域區域號碼（區域狀態!B 欄）→ DB 的 lookup：先比對 blockCode（A-x = nanzih、B-x = chiaotou、C-x = tzuguan），同 block 內按 mapAreaId 排序對應。個人區域=1、海總=2,3（nanzih）。

**操作現況（2026-08-16 cHinL 確認）**：
- 「區域狀態」C/D 欄（負責弟兄/分發日期）過去都是 **cHinL 一人手動維護**
- 傳道員完成後填 Google Form → append 到「傳道區域回報」→ E/F/G 公式自動算現況

**同步語意（待 grill 確認）**：
- Sheet「區域狀態」C/D 欄（負責弟兄/分發日期）↔ DB `assignedMemberId`/`dispatchedAt` — 雙向
- Sheet「傳道區域回報」append-only 流水 ↔ DB `completedAt` 紀錄 — 單向匯入


- 2026-09-22：使用者要求新增「刪除區塊」。選取後按鈕直接刪除整個候選（含內洞及多部件），可復原／重做，按儲存才寫入草稿；保留原始編號位置並重算未配對摘要。允許空候選草稿，以支援刪除最後一塊。

- 2026-09-22：框選頂點僅限目前候選區塊；使用者要求平滑時同時圓滑邊界與減少頂點，並可批次刪除。提供強度、保留未選頂點、每環至少三點；批次驗證失敗整筆不套用，可復原／重做，按儲存才寫入草稿。詳見 ADR-0011。
