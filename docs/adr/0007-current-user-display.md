# ADR-0007: 共用版面顯示登入者與角色

日期：2026-09-20
狀態：已接受

## 決策

使用者要求介面顯示登入者及權限。DashboardLayout 共用 `/api/me` 的成員姓名與 role，桌面右上方使用精簡狀態列，手機放在既有頂部列；不受側欄收合影響，不顯示 UID。

admin 顯示管理員、elder 顯示長老、publisher 顯示一般成員；未知角色顯示未識別角色，不推測權限。載入、失敗及登入失效各自明確顯示，失敗可重試；視窗重新取得焦點時刷新。

僅顯示角色，不更改任何 API 的授權規則。沿用現有身分 API，不新增資料庫欄位。
